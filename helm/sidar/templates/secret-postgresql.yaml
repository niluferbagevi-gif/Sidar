{{- if and .Values.postgresql.enabled (not .Values.postgresql.existingSecret.name) -}}
{{- fail "postgresql.existingSecret.name must reference a Kubernetes Secret or ExternalSecret-managed Secret containing DATABASE_URL, POSTGRES_DB, POSTGRES_USER, and POSTGRES_PASSWORD; inline PostgreSQL password values are intentionally unsupported" -}}
{{- end -}}
