# ─────────────────────────────────────────────
# Sidar — Yerel geliştirme ortamı şablonu
#
# Kullanım:
#   cp .env.example .env
#   cp .env.development.example .env.development
#   SIDAR_ENV=development uv run python main.py
#
# Notlar:
# - .env.development git tarafından izlenmez; gerçek sırları yalnız o dosyada tutun.
# - PostgreSQL parolasını yalnız POSTGRES_PASSWORD alanında değiştirin; uygulama
#   DATABASE_URL değerini POSTGRES_* bileşenlerinden otomatik türetir.
# - Docker Compose uygulama servisleri POSTGRES_HOST=postgres override eder; ayrıca
#   SIDAR_CONTAINER_DATABASE_URL yazmanız gerekmez.
# - Güçlü parola üretmek için: uv run python -c "import secrets; print(secrets.token_urlsafe(32))"
# ─────────────────────────────────────────────

SIDAR_ENV=development
AI_PROVIDER=ollama
CODING_MODEL=qwen2.5-coder:7b
OLLAMA_URL=http://localhost:11434/api
# Ollama server paralel istek limiti; GPU benchmark concurrency için >=4 önerilir.
OLLAMA_NUM_PARALLEL=4
OLLAMA_TIMEOUT=600

# ─── PostgreSQL ──────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=sidar
POSTGRES_USER=sidar
POSTGRES_PASSWORD=replace-with-a-strong-24-plus-character-password

# DATABASE_URL, SIDAR_CONTAINER_DATABASE_URL ve SELF_HEAL_DATABASE_URL normal geliştirme
# akışında elle yazılmamalıdır. Gerekirse yalnız ileri seviye override olarak .env içinde tanımlayın.

DB_DEGRADED_MODE_ON_POSTGRES_FAILURE=true
DB_SCHEMA_VERSION_TABLE=schema_versions

# Yerel geliştirmede auth katmanı kapatılmasın diye sabit olmayan bir değer verin.
API_KEY=replace-with-a-local-development-api-key
ACCESS_LEVEL=sandbox
