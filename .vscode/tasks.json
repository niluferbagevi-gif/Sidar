{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Auto: Activate venv + run tests",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "if [ -f .venv/bin/activate ]; then source .venv/bin/activate; elif [ -f venv/bin/activate ]; then source venv/bin/activate; else echo 'No virtual environment found (.venv or venv).'; exit 1; fi; pytest -q"
      ],
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      },
      "problemMatcher": []
    }
  ]
}
