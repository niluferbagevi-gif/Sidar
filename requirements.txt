# Sidar dependency entrypoint
#
# This file intentionally avoids pinning transitive dependencies.
# Use pyproject.toml as the single source of truth and install
# the project in editable mode.
#
# Examples:
#   uv pip install -r requirements.txt
#   uv pip install -e '.[dev]'
#   uv pip install -e '.[rag,gpu]'
-e .
